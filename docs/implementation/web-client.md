# Web 客户端当前实现说明

本文档记录 `apps/web` 当前已经落地的实现，用于补充总览文档中不适合继续堆叠的前端细节。

相关代码入口：

- [apps/web/src/App.vue](/home/etnatker/workspace/code/ff14arena_next/apps/web/src/App.vue)
- [apps/web/src/stores/app.ts](/home/etnatker/workspace/code/ff14arena_next/apps/web/src/stores/app.ts)
- [apps/web/src/components/BattleStage.vue](/home/etnatker/workspace/code/ff14arena_next/apps/web/src/components/BattleStage.vue)

## 1. 页面流转

当前客户端包含以下页面：

- 首页
- 房间大厅页
- 战斗页
- 结算页

当前页面切换规则如下：

- 未进入房间时显示首页
- 房间处于 `lobby` 时显示房间大厅页
- 房间处于 `loading` 或 `running` 时显示战斗页
- 房间处于 `finished` 且客户端已经持有最终结果时显示结算页

当前实现已经修正以下行为：

- 创建房间并加入时，会在进入新房间前清理旧的战斗快照与结算结果
- `sim:start` 与 `sim:restart` 到达时，会重置本地输入序号与战斗日志
- 客户端只在收到最终完成结果后切换到结算页
- 写入失败标记后，战斗页继续显示，失败原因仍留在 HUD 中持续展示

## 2. 当前操控模式

当前客户端已经实现两种本地可切换的操控模式：

- 传统
- 标准

该设置保存在浏览器本地存储中。

### 传统模式

当前行为如下：

- 左键拖拽改变镜头朝向
- 右键拖拽改变镜头朝向
- 左键和右键拖拽都不会直接改变人物面向
- `WASD` 移动方向以当前镜头朝向为准
- 人物在移动时自动朝向当前移动方向

### 标准模式

当前行为如下：

- 左键拖拽只改变镜头朝向
- 右键拖拽同时改变镜头朝向和人物朝向
- 右键拖拽时，人物面向与当前镜头方向保持一致
- `WASD` 移动方向以人物当前面向为准
- 移动本身不会修改人物面向

### 通用输入

当前共同规则如下：

- 键盘 `1` 触发防击退
- 移动输入按固定间隔连续发送
- 朝向输入在本地状态变化时发送

## 3. 当前镜头与绘制

当前战斗场地绘制使用 PixiJS，并已实现以下镜头行为：

- 镜头朝向由鼠标拖拽控制
- 战斗开始或重开时，初始镜头朝向与当前受控角色面向一致
- 鼠标滚轮控制缩放
- 双击场地重置缩放到默认值
- 当前受控玩家始终绘制在画面中心点偏下 `1/6` 的位置
- 当没有可控角色时，画面回到场地中心绘制

当前场地会绘制：

- 场地边界
- Boss 目标环
- Boss 本体
- 玩家与 Bot 位置点
- 朝向指示线
- 圆形 / 环形 / 分摊 / 分散 AOE

## 4. 当前 HUD 与战斗信息

当前 HUD 已显示以下内容：

- 战斗时间
- Boss 当前读条名
- 读条总时长
- 读条进度
- 战斗提示文本
- 中心提示文本
- 倒计时文本
- 失败原因聚合
- 当前操作模式
- 当前镜头缩放值

当前队伍面板已显示：

- 槽位名
- 成员名
- 当前 HP
- 存活状态
- 当前状态列表
- 防击退状态
- 防击退冷却

当前房间大厅槽位面板已显示：

- 槽位名
- 当前占用类型
- 玩家或 Bot 名称
- 在线状态
- 准备状态
- 当前 HP
- 存活状态
- 防击退状态

## 5. 当前仍未实现的前端项

以下项目在设计文档中已有目标描述，但当前前端仍未落地：

- 聊天与系统消息面板
- 房主踢人
- 手动补 Bot 与移除 Bot
- 手动调整槽位
- 更完整的镜像修正与平滑
- 更细致的战斗表现与日志反馈

其中前四项当前不仅缺少前端页面，还缺少对应的服务端协议与房间操作接口。
