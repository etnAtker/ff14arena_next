# Web 客户端当前实现说明

本文档记录 `apps/web` 当前已经落地的实现，用于补充总览文档中不适合继续堆叠的前端细节。

相关代码入口：

- [apps/web/src/App.vue](/home/etnatker/workspace/code/ff14arena_next/apps/web/src/App.vue)
- [apps/web/src/stores/app.ts](/home/etnatker/workspace/code/ff14arena_next/apps/web/src/stores/app.ts)
- [apps/web/src/components/battle/BattleStage.vue](/home/etnatker/workspace/code/ff14arena_next/apps/web/src/components/battle/BattleStage.vue)

## 1. 页面流转

当前客户端包含以下页面：

- 首页
- 模拟页

当前页面切换规则如下：

- 未进入房间时显示首页
- 进入任意房间后直接显示模拟页

当前实现已经修正以下行为：

- 创建房间并加入时，会在进入新房间前清理旧的房间快照与日志
- 进入房间后立即接收等待态快照并显示场地
- `sim:start` 到达时，会重置本地输入序号与战斗日志
- 战斗结束后不切换独立结算页，而是在右侧栏展示上一轮结果

## 2. 当前 UI 组织

当前前端页面壳层已切换为以下分工：

- `App.vue` 负责 provider、顶层状态、键盘输入与页面切换
- `components/layout` 负责顶栏等全局壳层组件
- `components/pages` 负责首页和模拟页页面组件
- `components/battle` 负责战斗场地和镜头换算等战斗视图模块
- `utils/ui.ts` 负责 phase 标签、槽位颜色、角色简写等展示辅助逻辑
- 页面组件与战斗视图按需异步加载，降低首页首包体积
- 首屏完成后，客户端会在后台预加载其余页面模块、战斗渲染模块和 Socket 客户端依赖
- Socket 客户端仍在首次需要联机时才真正创建连接，而不是应用启动时立即连服
- Naive UI 负责卡片、表单、选择器、按钮、标签、列表、描述信息和滚动容器
- 少量手写 CSS 只保留整体间距、响应式折叠和 Pixi 场地容器尺寸控制
- PixiJS 继续只负责战斗场地绘制与镜头交互

当前已经完成的 UI 重构点：

- 首页创建房间表单改为组件化表单
- 房间列表改为卡片和状态标签展示
- 模拟页改为左人员、中场地、右结果日志三栏布局
- 原先大厅页和独立结算页职责已并入模拟页

## 3. 当前操控模式

当前客户端已经实现两种本地可切换的操控模式：

- 传统
- 标准

该设置保存在浏览器本地存储中。

### 传统模式

当前行为如下：

- 左键拖拽改变镜头朝向
- 右键拖拽改变镜头朝向
- 左键和右键拖拽都不会直接改变人物面向
- `WASD` 移动方向以当前镜头朝向为准
- 人物在移动时自动朝向当前移动方向

### 标准模式

当前行为如下：

- 左键拖拽只改变镜头朝向
- 右键拖拽同时改变镜头朝向和人物朝向
- 右键拖拽时，人物面向与当前镜头方向保持一致
- `WASD` 移动方向以人物当前面向为准
- 移动本身不会修改人物面向

### 通用输入

当前共同规则如下：

- 键盘 `1` 触发防击退
- 移动输入按固定间隔连续发送
- 朝向输入在本地状态变化时发送

当前输入发送策略仍存在一项待优化点：

- `move`、`face` 与离散操作当前仍分别发送，没有统一的客户端输入帧
- 当前右键转向已补本地预览与节流发送，但移动与转向的发送策略仍分散在不同调用链中
- 后续建议统一为固定频率的连续输入帧，只把 `move`、`face` 这类可覆盖状态合并发送
- 房间操作与防击退这类一次性命令仍保持即时发送，不并入连续输入帧
- 该优化的主要收益是收敛客户端输入逻辑、减少高频小包数量、统一服务端消费顺序，而不是解决当前正确性问题

## 4. 当前镜头与绘制

当前战斗场地绘制使用 PixiJS，并已实现以下镜头行为：

- 镜头朝向由鼠标拖拽控制
- 进入房间或重新开始时，初始镜头朝向与当前受控角色面向一致
- 鼠标滚轮控制缩放
- 双击场地重置缩放到默认值
- 当前受控玩家始终绘制在画面中心点偏下固定位置
- 当没有可控角色时，画面回到场地中心绘制

当前场地会绘制：

- 场地边界
- Boss 目标环
- Boss 本体
- 玩家与 Bot 位置点及职能简写
- 朝向指示线
- 圆形 / 环形 / 分摊 / 分散 AOE
- 顶部 Boss 读条覆盖层

## 5. 当前 HUD 与战斗信息

当前模拟页左栏已显示：

- 8 个固定槽位卡片
- 槽位名、成员名与切换 / 准备 / 开始按钮
- 当前 HP / 最大 HP
- 按职能区分的底色与自己的紫色强调

当前模拟页右栏已显示：

- 上一轮结果摘要
- 失败原因列表
- 当前 8 人状态摘要
- 实时战斗日志

当前等待态与战斗中已统一使用同一套 `SimulationSnapshot`：

- `waiting` 阶段用于进房预览、切槽和等待开始
- `running` 阶段用于权威模拟
- `latestResult` 用于右侧栏展示上一轮结果

## 6. 当前仍未实现的前端项

以下项目在设计文档中已有目标描述，但当前前端仍未落地：

- 聊天与系统消息面板
- 更完整的镜像修正与平滑
- 更细致的战斗表现与日志反馈
- 连续输入统一打包发送
- 统一 `waiting` 与 `running` 两阶段的客户端同步流程

其中聊天与更细致表现当前仍缺少前后端完整支持。
